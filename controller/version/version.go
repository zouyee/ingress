package version

var (
	// RELEASE returns the release version
	RELEASE = "0.5"
	// REPO returns the git repository URL
	REPO = "github.com/zouyee/ingress"
	// COMMIT returns the short sha from git
	COMMIT = "UNKNOWN"
)
